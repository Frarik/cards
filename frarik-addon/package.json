{
  "name": "frarik-addon-server",
  "version": "1.0.0",
  "description": "Frarik Dashboard — HA Add-on server",
  "main": "server.js",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
